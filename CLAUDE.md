# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 提供在此仓库中工作的指导。

## 项目概述

这是一个基于 **React + Vite** 的数据可视化大屏项目，展示**全国各省产业现状**。使用 ECharts 渲染 2D 交互式中国地图，左右两侧信息卡片展示各省份详细产业经济数据。

## 可用命令

| 命令 | 说明 |
|---------|-------------|
| `npm run dev` | 启动开发服务器 (Vite) |
| `npm run build` | 构建生产版本 |
| `npm run lint` | 运行 ESLint 代码检查 |
| `npm run preview` | 本地预览生产构建 |

## 架构

### 技术栈
- **框架**: React 19 + Vite
- **地图可视化**: ECharts 6
- **样式**: CSS，采用赛博朋克/数据大屏风格

### 组件结构

**主布局** (`App.jsx`):
- 全屏仪表盘容器，科幻深色背景
- 顶部标题 "全国各省产业现状"，副标题 "INDUSTRIAL DISTRIBUTION OF CHINA"
- 三栏布局：左侧两个信息卡片 + 中间地图 + 右侧两个信息卡片

**核心组件** (位于 `src/components/`):

| 组件 | 用途 |
|-----------|---------|
| `ChinaMap3D.jsx` | **唯一主组件**，包含 2D 中国地图 + 四个信息卡片。从阿里云 DataV 加载 GeoJSON，支持鼠标交互和自动轮播。（组件名称保留 3D 是历史原因，实际使用 ECharts 2D 渲染） |

### 布局与功能详情

**整体布局**:
```
┌─────────────────┬───────────────────┬─────────────────┐
│  左上：基本信息 │                   │ 右上：GDP对比   │
│  • 常住人口     │                   │  • GDP排名      │
│  • 2024年GDP   │                   │  • GDP总量      │
│  • GDP排名     │       中国地图     │  • 人均GDP      │
│  • 发展潜力     │                   │  • 占全国比重    │
├─────────────────┤                   ├─────────────────┤
│  左下：产业概况 │                   │ 右下：产业升级  │
│  • 主要产业标签 │                   │  • 转型方向     │
│  • 经济特点     │                   │  • 新兴产业     │
│  • 发展定位     │                   │  • 重点园区     │
│  • 年轻人前景   │                   │  • 政策支持     │
└─────────────────┴───────────────────┴─────────────────┘
```

**地图配置**:
- 禁用缩放、拖拽、旋转 (`roam: false`)
- 地图放大比例 `zoom: 1.2`，居中显示
- 排除南海诸岛不展示
- 热力图按 GDP 数值着色（从低到高：深蓝 → 浅蓝 → 橙 → 红）
- 悬浮气泡**持续显示**省份名称、主要产业、GDP 规模

**交互逻辑**:
1. **点击省份**: 停止自动轮播，切换卡片信息并持续高亮显示该省份数据
2. **鼠标移出地图**: 从当前位置恢复自动轮播，每 **3 秒** 自动切换一个省份
3. **自动轮播运行期间**: 卡片信息和地图高亮随轮播自动更新
4. **初始化**: 默认高亮并显示 **北京市** 信息，启动自动轮播

**内容详情**:

| 卡片区域 | 内容 |
|---------|------|
| **左上 - 基本信息** | 省份名称、常住人口（万人）、2024年GDP（亿元）、GDP全国排名、发展潜力等级（A+, A, A-, B+, B, B-） |
| **左下 - 产业概况** | 主要产业标签（彩色胶囊样式）、经济特点描述、发展定位（按潜力等级）、年轻人发展前景（默认两行截断，鼠标悬停显示完整） |
| **右上 - GDP排名对比** | GDP排名、GDP总量、常住人口、人均GDP、占全国比重、增速等级（高速增长/较快增长/平稳增长/稳步调整） |
| **右下 - 产业升级方向** | 重点转型方向、新兴产业布局、重点投资园区、政策支持力度 |

### 数据

**数据来源**:
- GDP、人口数据：**2024年各省统计局公开真实数据**（已核对排名）
- 共包含 33 个省级行政区完整数据
- GeoJSON 地图边界：从阿里云 DataV `https://geo.datav.aliyun.com/areas_v3/bound/100000_full.json` 动态加载

### 样式规范

仪表盘采用赛博朋克/科幻风格:
- 背景: 深蓝色渐变 (`#000811` → `#001525` → `#001020`)
- 强调色:
  - 边框/标题: 青色 (`#00ffff`)
  - GDP数值: 红色 (`#ff0000`)
  - 排名: 黄色 (`#ffff00`)
  - 人均GDP: 青绿色 (`#00ffaa`)
- 玻璃拟态面板: 使用 `rgba()` 半透明背景 + `backdrop-filter: blur` 模糊效果
- CSS 动画: 边框脉冲发光效果，标题渐变颜色动画

### 重要提示

- ECharts 地图需要网络连接才能从阿里云 DataV 加载 GeoJSON 数据
- GDP 数据和排名已核对，确保准确无误
- 年轻人前景文字使用 `-webkit-line-clamp: 2` 实现默认两行截断，完整内容通过 `title` 属性显示
- ESLint 配置忽略 `dist/` 目录，使用推荐的 React Hooks 规则
